# kids.spirzen.ru — детский раздел «Вселенная IT»

Astro-портал (заготовка). Один repo = один домен (GitHub Pages).

| | |
|---|---|
| Прод | kids.spirzen.ru |
| Локально | http://localhost:4333 |

```bash
npm install
npm run dev
npm run build
```

Соседние порталы: `it-terms`, `it-lab`, `it-games`, `it-tools`. Общий nav: `ecosystem-urls.json`.
